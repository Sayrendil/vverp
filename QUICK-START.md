# 🚀 Быстрый старт развертывания VVERP

## Что исправлено:

✅ **Порт изменен**: `80` → `8041` (нет конфликта)
✅ **Dockerfile упрощен**: Используется проверенная конфигурация из рабочего проекта
✅ **Workdir исправлен**: `/var/www/html` → `/var/www`
✅ **User правильный**: Через переменные окружения UID/GID
✅ **Docker зеркала**: Настроены в daemon.json для быстрой загрузки образов

---

## 📦 Развертывание на сервере

### Метод 1: Автоматический (рекомендуется)

```bash
cd /home/erp/vverp

# Запустите автоматический скрипт
chmod +x DEPLOY-FINAL.sh
sudo ./DEPLOY-FINAL.sh
```

Скрипт автоматически:
1. Очистит Docker кэш
2. Загрузит базовый образ PHP
3. Соберет контейнеры
4. Запустит приложение

**Время: ~5-10 минут**

---

### Метод 2: Ручной (пошаговый контроль)

```bash
cd /home/erp/vverp

# 1. Очистите кэш
sudo docker builder prune -af

# 2. Загрузите базовый образ
sudo docker pull php:8.2-fpm

# 3. Соберите образы
sudo docker compose build --no-cache

# 4. Запустите контейнеры
sudo docker compose up -d

# 5. Проверьте статус
sudo docker compose ps
```

---

## 🔍 Проверка после развертывания

### Статус контейнеров

```bash
sudo docker compose ps
```

Должны быть запущены:
- ✅ `vverp_app` (PHP-FPM)
- ✅ `vverp_nginx` (веб-сервер на порту 8041)
- ✅ `vverp_db` (MySQL)
- ✅ `vverp_queue` (воркеры и Telegram бот)
- ✅ `vverp_node` (Vite для фронтенда)

### Проверка логов

```bash
# Все логи сразу
sudo docker compose logs -f

# Логи конкретных сервисов
sudo docker compose logs -f app
sudo docker compose logs -f queue
sudo docker compose logs -f nginx
```

### Проверка веб-доступа

```bash
# С сервера
curl http://localhost:8041

# Или откройте в браузере
http://your-server-ip:8041
```

---

## ⚙️ Первоначальная настройка Laravel

После успешного запуска контейнеров:

```bash
# 1. Войдите в контейнер приложения
sudo docker compose exec app bash

# 2. Установите зависимости Composer (если еще не установлены)
composer install

# 3. Сгенерируйте ключ приложения
php artisan key:generate

# 4. Запустите миграции
php artisan migrate

# 5. Создайте storage link
php artisan storage:link

# 6. Очистите кэш
php artisan config:clear
php artisan cache:clear
php artisan view:clear

# Выход из контейнера
exit
```

---

## 🐛 Устранение проблем

### Проблема: Контейнер не запускается

```bash
# Проверьте логи
sudo docker compose logs app

# Пересоберите без кэша
sudo docker compose build --no-cache app
sudo docker compose up -d app
```

### Проблема: Nginx показывает 502 Bad Gateway

```bash
# Проверьте, запущен ли PHP-FPM
sudo docker compose ps app

# Перезапустите контейнеры
sudo docker compose restart app nginx
```

### Проблема: Очень долгая сборка

Это нормально при первой сборке на сервере с ограниченным доступом к интернету.
Ожидаемое время: **5-15 минут** в зависимости от скорости интернета.

Если зависла на установке пакетов более 20 минут:
```bash
# Прервите (Ctrl+C) и попробуйте снова
sudo docker compose build --no-cache
```

---

## 📂 Важные пути

- **Код приложения**: `/var/www` (внутри контейнера)
- **Nginx конфиг**: `./docker/nginx/default.conf`
- **PHP конфиг**: `./docker/php/php.ini`
- **Supervisor конфиг**: `./docker/supervisor/supervisord.conf`
- **Данные MySQL**: `dbdata` volume

---

## 🔄 Команды для работы

```bash
# Перезапуск всех сервисов
sudo docker compose restart

# Перезапуск конкретного сервиса
sudo docker compose restart app

# Остановка
sudo docker compose stop

# Запуск
sudo docker compose start

# Полная остановка и удаление
sudo docker compose down

# Остановка с удалением volumes (ОСТОРОЖНО: удалит БД!)
sudo docker compose down -v
```

---

## ✨ Готово!

Приложение запущено и доступно на порту **8041**! 🎉

Следующие шаги:
1. Настройте `.env` файл
2. Настройте Telegram бот токен
3. Проверьте работу очередей
4. Настройте backup базы данных

---

## 📞 Полезные команды

```bash
# Войти в контейнер
sudo docker compose exec app bash

# Запустить artisan команду
sudo docker compose exec app php artisan [команда]

# Проверить очереди
sudo docker compose exec queue supervisorctl status

# Перезапустить воркеры
sudo docker compose exec queue supervisorctl restart all

# Просмотр логов Laravel
sudo docker compose exec app tail -f storage/logs/laravel.log
```
