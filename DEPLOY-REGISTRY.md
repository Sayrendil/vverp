# 🚀 Развертывание через GitHub Container Registry

Этот метод позволяет собрать образы локально и опубликовать их в GitHub, откуда сервер сможет их загрузить.

---

## 📋 Предварительные требования

1. **GitHub Personal Access Token** с правами `write:packages`
   - Создать: https://github.com/settings/tokens/new?scopes=write:packages
   - Скопируйте токен - он понадобится

2. **Публичный или приватный репозиторий** на GitHub
   - Если приватный - на сервере тоже нужен токен для pull

---

## 🏗️ Метод 1: Автоматическая сборка через GitHub Actions (РЕКОМЕНДУЕТСЯ)

### На локальной машине:

```bash
cd /home/sayrendil/vkusvill/vverp

# Закоммитьте все изменения
git add .
git commit -m "Add Docker images for production"
git push origin main
```

### Что происходит автоматически:

1. GitHub Actions запускает workflow
2. Собирает оба образа (app и queue)
3. Публикует в GitHub Container Registry
4. Образы доступны по адресам:
   - `ghcr.io/sayrendil/vverp-app:latest`
   - `ghcr.io/sayrendil/vverp-queue:latest`

### На сервере:

```bash
cd /home/erp/vverp

# Обновите код
git pull

# Если репозиторий приватный - логин в GHCR
echo "YOUR_GITHUB_TOKEN" | sudo docker login ghcr.io -u sayrendil --password-stdin

# Загрузите и запустите
sudo docker compose -f docker-compose.prod.yml pull
sudo docker compose -f docker-compose.prod.yml up -d
```

---

## 🔨 Метод 2: Ручная сборка и публикация

### На локальной машине (WSL):

```bash
cd /home/sayrendil/vkusvill/vverp

# Запустите скрипт сборки
chmod +x build-local.sh
./build-local.sh
```

Скрипт:
1. Попросит ваш GitHub Token
2. Соберет образы локально
3. Опубликует их в GitHub Container Registry

### На сервере:

```bash
cd /home/erp/vverp

# Запустите скрипт развертывания
chmod +x deploy-from-registry.sh
sudo ./deploy-from-registry.sh
```

---

## 🎯 Структура файлов

- `.github/workflows/build-and-push.yml` - GitHub Actions для автоматической сборки
- `docker-compose.prod.yml` - Конфигурация для production (использует готовые образы)
- `build-local.sh` - Скрипт для ручной локальной сборки
- `deploy-from-registry.sh` - Скрипт для развертывания на сервере

---

## 🔐 Настройка прав доступа

### Если репозиторий публичный:

Образы будут публично доступны для pull (но не для push).

### Если репозиторий приватный:

1. На GitHub:
   - Settings → Packages → Package settings
   - Change visibility → Public (или оставьте Private)

2. На сервере для приватных образов:
```bash
# Один раз настроить
echo "YOUR_TOKEN" | sudo docker login ghcr.io -u sayrendil --password-stdin

# Сохранится в /root/.docker/config.json
```

---

## 📊 Преимущества этого метода

✅ **Быстро**: Сервер только загружает готовые образы (1-2 минуты)
✅ **Надежно**: Не зависит от сетевых проблем сервера при сборке
✅ **Повторяемо**: Один раз собрали - можно разворачивать на любых серверах
✅ **CI/CD**: Автоматическая сборка при push в main
✅ **Версионирование**: Можно хранить разные версии образов

---

## 🔄 Обновление приложения

После изменений в коде:

```bash
# На локальной машине
git add .
git commit -m "Update application"
git push

# Подождите пока GitHub Actions соберет образы (~5-10 минут)
# Проверить можно здесь: https://github.com/Sayrendil/vverp/actions

# На сервере
cd /home/erp/vverp
git pull
sudo docker compose -f docker-compose.prod.yml pull
sudo docker compose -f docker-compose.prod.yml up -d
```

---

## 🐛 Troubleshooting

### Ошибка: "unauthorized: authentication required"

```bash
# Залогиньтесь в GHCR
echo "YOUR_TOKEN" | sudo docker login ghcr.io -u sayrendil --password-stdin
```

### Ошибка: "manifest unknown"

Образ еще не опубликован. Проверьте:
1. GitHub Actions завершился успешно
2. Образы появились в https://github.com/Sayrendil?tab=packages

### GitHub Actions не запускается

1. Проверьте что workflow файл в `.github/workflows/`
2. Actions должны быть включены в Settings → Actions → General

---

## 📦 Размер образов

Примерные размеры:
- `vverp-app`: ~400-600 MB
- `vverp-queue`: ~400-600 MB

Время загрузки на сервере: **1-3 минуты** (в зависимости от интернета)

---

## ✨ Готово!

Теперь у вас полноценный CI/CD pipeline:
1. Разработка локально
2. Push в GitHub
3. Автоматическая сборка образов
4. Быстрое развертывание на любом сервере

🎉 **Успешного развертывания!**
