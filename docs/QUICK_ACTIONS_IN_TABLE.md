# ⚡ Быстрые действия в таблице заявок

## 📋 Обзор

В таблице заявок (`/tickets`) добавлены быстрые кнопки управления, позволяющие выполнять действия без перехода на страницу просмотра заявки.

---

## 🎯 Доступные быстрые действия

### 1. ✅ Взять в работу (Зелёная)

**Когда показывается:**
- Статус заявки: **"Создана"** И нет исполнителя
- Статус заявки: **"Отложена"** И (пользователь исполнитель ИЛИ нет исполнителя)

**Кто видит:**
- Исполнители категории
- Администраторы категории

**Что делает:**
- Назначает текущего пользователя исполнителем
- Меняет статус на "В работе"
- Страница автоматически обновляется

**Цвет:** `text-green-400` hover `text-green-300`

---

### 2. ⏸️ Отложить (Оранжевая)

**Когда показывается:**
- Статус заявки: **"Создана"** И пользователь исполнитель категории
- Статус заявки: **"В работе"** И пользователь назначенный исполнитель

**Кто видит:**
- Исполнители категории (для статуса "Создана")
- Назначенный исполнитель (для статуса "В работе")

**Что делает:**
- Меняет статус на "Отложена"
- Исполнитель остаётся назначенным (если был)
- Страница автоматически обновляется

**Цвет:** `text-orange-400` hover `text-orange-300`

---

### 3. ✉️ Отправить на подтверждение (Синяя)

**Когда показывается:**
- Статус заявки: **"В работе"**
- Пользователь является назначенным исполнителем

**Кто видит:**
- Только назначенный исполнитель

**Что делает:**
- Меняет статус на "Подтверждена"
- Отправляет Telegram уведомление автору
- Страница автоматически обновляется

**Цвет:** `text-blue-400` hover `text-blue-300`

---

## 🎨 Визуальное оформление

### Расположение кнопок

```
┌─────────────────────────────────────────────────┐
│ [✅] [⏸️] [✉️] │ [👁] [✏️]                     │
│  ↑    ↑    ↑   ↑   ↑   ↑                        │
│  │    │    │   │   │   └─ Редактировать        │
│  │    │    │   │   └───── Просмотр             │
│  │    │    │   └───────── Разделитель          │
│  │    │    └───────────── Отправить            │
│  │    └────────────────── Отложить             │
│  └─────────────────────── Взять в работу       │
└─────────────────────────────────────────────────┘
```

### Цветовая схема

| Действие | Цвет | Hex |
|----------|------|-----|
| Взять в работу | Зелёный | `#4ade80` |
| Отложить | Оранжевый | `#fb923c` |
| Отправить на подтверждение | Синий | `#60a5fa` |
| Просмотр | Серый → Зелёный | `#9ca3af` → `#4ade80` |
| Редактировать | Серый → Синий | `#9ca3af` → `#60a5fa` |

### Hover эффекты

- Цвет текста становится светлее
- Появляется полупрозрачный фон цвета кнопки
- Плавная анимация (`transition-colors`)

---

## 💻 Техническая реализация

### Vue.js - проверки прав

```javascript
const canTakeToWork = (ticket) => {
    const isExecutorOfCategory = ticket.ticketCategory?.id === user.value.ticket_category_id
        || (user.value.role === 'admin' && user.value.ticket_category_id === ticket.ticketCategory?.id);

    const isExecutor = ticket.executor?.id === user.value.id;

    const statusCreated = ticket.status?.name === 'Создана' && !ticket.executor && isExecutorOfCategory;
    const statusPostponed = ticket.status?.name === 'Отложена'
        && (isExecutor || (!ticket.executor && isExecutorOfCategory));

    return statusCreated || statusPostponed;
};

const canPostpone = (ticket) => {
    const isExecutor = ticket.executor?.id === user.value.id;
    const isExecutorOfCategory = ticket.ticketCategory?.id === user.value.ticket_category_id
        || (user.value.role === 'admin' && user.value.ticket_category_id === ticket.ticketCategory?.id);

    const statusCreated = ticket.status?.name === 'Создана' && isExecutorOfCategory;
    const statusInProgress = isExecutor && ticket.status?.name === 'В работе';

    return statusCreated || statusInProgress;
};

const canSendForConfirmation = (ticket) => {
    const isExecutor = ticket.executor?.id === user.value.id;
    return isExecutor && ticket.status?.name === 'В работе';
};
```

### Методы быстрых действий

```javascript
const quickTakeToWork = (ticketId) => {
    router.post(route('tickets.take-to-work', ticketId), {}, {
        preserveScroll: true, // Сохраняем позицию скролла
        onSuccess: () => {
            // Inertia автоматически обновит данные
        }
    });
};

const quickPostpone = (ticketId) => {
    router.post(route('tickets.postpone', ticketId), {}, {
        preserveScroll: true,
    });
};

const quickSendForConfirmation = (ticketId) => {
    router.post(route('tickets.send-for-confirmation', ticketId), {}, {
        preserveScroll: true,
    });
};
```

---

## 🎭 Примеры использования

### Сценарий 1: Быстрое взятие в работу

```
1. Пользователь (исполнитель категории) открывает список заявок
2. Видит заявку со статусом "Создана"
3. Видит зелёную кнопку ✅ рядом со стандартными кнопками
4. Нажимает на неё (без перехода на страницу заявки)
5. Заявка мгновенно меняет статус на "В работе"
6. Пользователь назначается исполнителем
7. Список обновляется, кнопка ✅ исчезает, появляются ⏸️ и ✉️
```

### Сценарий 2: Быстрое откладывание

```
1. Исполнитель видит заявку "В работе", которая назначена на него
2. Видит оранжевую кнопку ⏸️
3. Понимает, что нужны запчасти
4. Нажимает кнопку ⏸️ прямо в таблице
5. Статус меняется на "Отложена"
6. Список обновляется, кнопка меняется на ✅ (взять в работу)
```

### Сценарий 3: Отправка на подтверждение

```
1. Исполнитель выполнил работу
2. Открывает список заявок
3. Видит свою заявку "В работе" с синей кнопкой ✉️
4. Нажимает на неё
5. Статус меняется на "Подтверждена"
6. Автор получает Telegram уведомление
7. Список обновляется, кнопки исчезают (кроме просмотра)
```

---

## 📊 Матрица кнопок в таблице

| Статус заявки | Роль пользователя | Быстрые кнопки | Стандартные кнопки |
|---------------|-------------------|----------------|--------------------|
| **Создана** | Исполнитель категории | ✅ ⏸️ | 👁 ✏️ |
| **Создана** | Другой пользователь | - | 👁 ✏️ |
| **В работе** | Назначенный исполнитель | ⏸️ ✉️ | 👁 ✏️ |
| **В работе** | Другой пользователь | - | 👁 ✏️ |
| **Подтверждена** | Любой | - | 👁 ✏️ |
| **Отложена** | Назначенный исполнитель | ✅ | 👁 ✏️ |
| **Отложена** | Исполнитель категории (без исполнителя) | ✅ | 👁 ✏️ |
| **Отложена** | Другой пользователь | - | 👁 ✏️ |
| **Завершена** | Любой | - | 👁 |

*Примечание:* ✏️ (редактировать) не показывается для статуса "Завершена"

---

## ⚡ Преимущества быстрых кнопок

### 1. **Скорость работы**
- Не нужно открывать заявку для простых действий
- Экономия 2-3 кликов на каждое действие
- Мгновенная обратная связь

### 2. **Удобство**
- Все действия на одной странице
- Не теряется контекст работы
- Позиция скролла сохраняется

### 3. **Наглядность**
- Сразу видно доступные действия для каждой заявки
- Цветовое кодирование помогает различать типы действий
- Tooltip подсказывает назначение кнопки

### 4. **Консистентность**
- Логика точно такая же, как на странице просмотра
- Те же проверки прав доступа
- Те же backend методы

---

## 🔒 Безопасность

### Backend проверки

Все быстрые действия используют **те же контроллеры и проверки**, что и действия на странице просмотра:

```php
// TicketController@takeToWork
public function takeToWork(string $id)
{
    $ticket = $this->ticketService->getTicket($id);
    // ... проверки прав ...
    $this->ticketService->assignExecutor($ticket, $user->id);
}
```

### Frontend проверки

Кнопки показываются только когда:
1. Пользователь имеет нужные права
2. Статус заявки позволяет действие
3. Пользователь относится к нужной категории

**Двойная защита:**
- Frontend скрывает кнопки (UX)
- Backend проверяет права (Security)

---

## 🚀 Будущие улучшения

- [ ] Анимация изменения статуса (без перезагрузки строки)
- [ ] Подтверждающие модалки для критичных действий
- [ ] Bulk actions (массовое взятие в работу)
- [ ] Drag & Drop для назначения исполнителей
- [ ] Уведомления toast о результате действия
- [ ] История быстрых действий в activity log

---

## 📝 Итог

Быстрые кнопки в таблице заявок значительно ускоряют работу исполнителей, позволяя управлять заявками без лишних переходов. Цветовое кодирование и интуитивные иконки делают интерфейс понятным и удобным.

**Основные действия:**
- ✅ **Зелёная** - Взять в работу
- ⏸️ **Оранжевая** - Отложить
- ✉️ **Синяя** - Отправить на подтверждение
