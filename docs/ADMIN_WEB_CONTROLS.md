# 🎛️ Административное управление заявками через Web-интерфейс

## 📋 Обзор

Добавлен полный набор кнопок управления заявками для администраторов в веб-интерфейсе, аналогичный функционалу Telegram бота.

## ✨ Новые возможности для администраторов

### 1. **Панель управления администратора**

На странице просмотра заявки (`/tickets/{id}`) администраторы категории видят специальную панель управления с фиолетовым акцентом.

**Доступ:**
- Только для пользователей с ролью `admin`
- Только для заявок своей категории (`user.ticket_category_id === ticket.ticket_category_id`)

### 2. **Назначение исполнителя**

**Функционал:**
- Назначить исполнителя из списка активных исполнителей категории
- Сменить текущего исполнителя
- Снять исполнителя с заявки

**Кнопки:**
- `Назначить` / `Сменить` - открывает модальное окно с выбором исполнителя
- `Снять` - снимает текущего исполнителя (доступна только если исполнитель назначен)

**Логика:**
- При назначении исполнителя на заявку со статусом "Создана", статус автоматически меняется на "В работе"
- При снятии исполнителя с заявки со статусом "В работе", статус возвращается в "Создана"

**Маршрут:**
```php
POST /tickets/{id}/assign-executor
POST /tickets/{id}/unassign-executor
```

### 3. **Изменение статуса**

**Функционал:**
- Принудительное изменение статуса заявки на любой доступный
- Полный контроль над жизненным циклом заявки

**Кнопка:**
- `Изменить статус` - открывает модальное окно с выбором статуса

**Доступные статусы:**
- Создана
- В работе
- Подтверждена
- Отложена
- Завершена

**Маршрут:**
```php
POST /tickets/{id}/admin-change-status
```

### 4. **Информационный блок**

Показывает администратору его права и категорию, к которой он привязан.

## 🎨 Визуальное оформление

### Панель администратора
- **Цвет:** Фиолетовый градиент (`purple-900/20` to `purple-800/20`)
- **Граница:** `border-purple-500/50`
- **Иконка:** Шестеренка настроек

### Кнопки
- **Основные действия:** Фиолетовый фон (`bg-purple-600`, `hover:bg-purple-700`)
- **Вторичные действия:** Серый фон (`bg-gray-700`, `hover:bg-gray-600`)

### Модальные окна
- **Граница:** `border-purple-700`
- **Заголовок:** С иконкой и фиолетовым акцентом
- **Стиль:** Consistent с общим дизайном системы

## 🔧 Технические детали

### Backend (Laravel)

**Новые методы в `TicketController`:**

```php
// Назначить исполнителя
public function assignExecutor(Request $request, string $id)

// Снять исполнителя
public function unassignExecutor(string $id)

// Изменить статус админом
public function adminChangeStatus(Request $request, string $id)
```

**Новые маршруты в `routes/web.php`:**

```php
Route::post('/tickets/{id}/assign-executor', [TicketController::class, 'assignExecutor'])
    ->name('tickets.assign-executor');

Route::post('/tickets/{id}/unassign-executor', [TicketController::class, 'unassignExecutor'])
    ->name('tickets.unassign-executor');

Route::post('/tickets/{id}/admin-change-status', [TicketController::class, 'adminChangeStatus'])
    ->name('tickets.admin-change-status');
```

**Обновление метода `show`:**
```php
// Для админов получаем список доступных исполнителей их категории
$availableExecutors = [];
if ($user->isAdmin() && $user->ticket_category_id === $ticket->ticket_category_id) {
    $availableExecutors = $ticket->ticketCategory
        ->activeExecutors()
        ->select('users.id', 'users.name', 'users.email')
        ->get()
        ->toArray();
}
```

### Frontend (Vue.js)

**Новые props в `Show.vue`:**
```javascript
availableExecutors: Array,  // Список доступных исполнителей
allStatuses: Array,         // Все доступные статусы
```

**Новые computed свойства:**
```javascript
const isAdmin = computed(() => {
    return user.value.role === 'admin'
        && user.value.ticket_category_id === props.ticket.ticket_category_id;
});
```

**Новые формы:**
```javascript
const assignExecutorForm = useForm({
    executor_id: null,
});

const changeStatusForm = useForm({
    status_id: props.ticket.status?.id || null,
});
```

**Новые методы:**
```javascript
const assignExecutor = () => { ... }
const unassignExecutor = () => { ... }
const changeStatus = () => { ... }
```

## 📊 Сравнение с Telegram ботом

| Функция | Telegram бот | Web-интерфейс | Статус |
|---------|--------------|---------------|--------|
| Взять в работу | ✅ | ✅ | Реализовано |
| Отложить | ✅ | ✅ | Реализовано |
| Назначить исполнителя | ✅ | ✅ | **Новое!** |
| Снять исполнителя | ✅ | ✅ | **Новое!** |
| Изменить статус | ✅ | ✅ | **Новое!** |
| Подтвердить выполнение | ✅ | ✅ | Реализовано |
| Вернуть в работу | ✅ | ✅ | Реализовано |

## 🔒 Безопасность

### Проверки прав доступа

**На уровне бэкенда:**
```php
// Проверяем, что пользователь админ своей категории
if (!$user->isAdmin() || $user->ticket_category_id !== $ticket->ticket_category_id) {
    return back()->with('error', 'У вас нет прав для...');
}
```

**На уровне фронтенда:**
```javascript
// Панель видна только админам своей категории
v-if="isAdmin"
```

### Валидация данных

```php
// Назначение исполнителя
$validated = $request->validate([
    'executor_id' => 'required|exists:users,id',
]);

// Изменение статуса
$validated = $request->validate([
    'status_id' => 'required|exists:statuses,id',
]);
```

## 🚀 Использование

### Для администратора категории

1. **Открыть заявку** своей категории
2. **Увидеть панель управления** с фиолетовым акцентом
3. **Выбрать действие:**
   - Назначить/сменить/снять исполнителя
   - Изменить статус заявки
4. **Подтвердить действие** в модальном окне

### Для обычного пользователя

- Панель управления администратора не отображается
- Доступны только стандартные кнопки действий (взять в работу, отложить и т.д.)

## 📝 Примечания

### Отличия от обычных кнопок действий

1. **Визуально отделена** - используется фиолетовый цвет вместо зеленого
2. **Более широкие права** - админ может управлять любой заявкой своей категории
3. **Дополнительный функционал** - назначение/снятие исполнителей, прямое изменение статуса

### Будущие улучшения

- [ ] История изменений (кто, когда, что изменил)
- [ ] Комментарии к административным действиям
- [ ] Уведомления пользователей об административных изменениях
- [ ] Массовое назначение исполнителей
- [ ] Фильтр заявок по исполнителю

## 🎯 Итог

Теперь административное управление заявками через веб-интерфейс полностью соответствует функционалу Telegram бота и даже превосходит его по удобству благодаря:

- Визуальному выбору из списков (dropdown)
- Модальным окнам с подтверждением
- Информативным подсказкам
- Красивому UI/UX оформлению
