# 🎛️ Полный Workflow управления заявками - Кнопки и логика

## 📋 Обзор

Полное описание кнопок управления заявками в зависимости от статуса и прав пользователя.

---

## 🔄 Workflow заявки

```
┌─────────────┐
│   СОЗДАНА   │ ← Новая заявка
└──────┬──────┘
       │
       ├─► ✅ "Взять в работу" (исполнитель категории)
       │
       ├─► ⏸️ "Отложить" (исполнитель категории)
       │
       ▼
┌─────────────┐
│  В РАБОТЕ   │ ← Взята исполнителем
└──────┬──────┘
       │
       ├─► ⏸️ "Отложить" (исполнитель)
       │
       ├─► ✉️ "Отправить на подтверждение" (исполнитель)
       │
       ▼
┌─────────────────┐
│ НА ПОДТВЕРЖДЕНИИ│ ← Ждёт подтверждения автора
└────────┬────────┘
         │
         ├─► ✅ "Подтвердить выполнение" (автор) → ЗАВЕРШЕНА
         │
         └─► ◀️ "Вернуть в работу" (автор) → В РАБОТЕ
```

```
┌─────────────┐
│  ОТЛОЖЕНА   │ ← Временно приостановлена
└──────┬──────┘
       │
       └─► ✅ "Взять в работу" (исполнитель или исполнитель категории)
              │
              ▼
         ┌─────────────┐
         │  В РАБОТЕ   │
         └─────────────┘
```

```
┌─────────────┐
│ ЗАВЕРШЕНА   │ ← Финальный статус
└─────────────┘
     ❌ Нет кнопок
     ❌ Нельзя редактировать
     ❌ Нельзя удалить
```

---

## 🎯 Кнопки по статусам

### 1. Статус: "Создана"

**Доступные действия:**

| Кнопка | Кто видит | Результат |
|--------|-----------|-----------|
| ✅ **Взять в работу** | Исполнители категории | Статус → "В работе"<br>Исполнитель назначается |
| ⏸️ **Отложить** | Исполнители категории | Статус → "Отложена"<br>Исполнитель НЕ назначается |

**Логика:**
```javascript
canTakeToWork = статус "Создана"
    && нет исполнителя
    && пользователь исполнитель категории

canPostpone = статус "Создана"
    && пользователь исполнитель категории
```

---

### 2. Статус: "В работе"

**Доступные действия:**

| Кнопка | Кто видит | Результат |
|--------|-----------|-----------|
| ⏸️ **Отложить** | Назначенный исполнитель | Статус → "Отложена"<br>Исполнитель остаётся |
| ✉️ **Отправить на подтверждение** | Назначенный исполнитель | Статус → "Подтверждена"<br>Автору отправляется Telegram уведомление |

**Логика:**
```javascript
canPostpone = пользователь исполнитель
    && статус "В работе"

canSendForConfirmation = пользователь исполнитель
    && статус "В работе"
```

**Telegram уведомление автору:**
```
✅ Заявка #{id} выполнена!

Исполнитель: {имя}
Категория: {категория}

Пожалуйста, проверьте и подтвердите выполнение.

[✅ Подтвердить выполнение] [◀️ Вернуть в работу]
```

---

### 3. Статус: "Подтверждена"

**Доступные действия:**

| Кнопка | Кто видит | Результат |
|--------|-----------|-----------|
| ✅ **Подтвердить выполнение** | Автор заявки | Статус → "Завершена"<br>Заявка закрывается |
| ◀️ **Вернуть в работу** | Автор заявки | Статус → "В работе"<br>Исполнитель продолжает работу |

**Логика:**
```javascript
canConfirmCompletion = пользователь автор
    && статус "Подтверждена"

canReturnToWork = пользователь автор
    && статус "Подтверждена"
```

---

### 4. Статус: "Отложена"

**Доступные действия:**

| Кнопка | Кто видит | Результат |
|--------|-----------|-----------|
| ✅ **Взять в работу** | Назначенный исполнитель ИЛИ<br>Исполнители категории (если нет исполнителя) | Статус → "В работе" |

**Логика:**
```javascript
canTakeToWork = статус "Отложена"
    && (
        пользователь исполнитель
        || (нет исполнителя && пользователь исполнитель категории)
    )
```

---

### 5. Статус: "Завершена"

**Доступные действия:**

| Действие | Доступность |
|----------|-------------|
| Кнопок управления | ❌ Нет |
| Редактирование | ❌ Запрещено (Policy) |
| Удаление | ❌ Запрещено (Policy) |

**В header вместо кнопок:**
```
┌──────────────────────────┐
│ ✅ Заявка завершена      │ ← Зелёный badge
└──────────────────────────┘
```

---

## 🎨 Административные кнопки

Администраторы категории видят дополнительную панель управления (фиолетовая):

### Всегда доступны (кроме "Завершена"):

| Кнопка | Действие | Ограничения |
|--------|----------|-------------|
| 👤 **Назначить исполнителя** | Выбор из списка активных исполнителей | - |
| 🔄 **Изменить статус** | Принудительная смена статуса | - |
| ❌ **Снять исполнителя** | Снятие текущего исполнителя | Только если есть исполнитель |

---

## 🔒 Права доступа

### Роли пользователей:

| Роль | Описание | Может |
|------|----------|-------|
| **admin** | Администратор категории | - Видит все заявки своей категории<br>- Назначает исполнителей<br>- Меняет статусы<br>- Редактирует любые заявки (кроме завершенных) |
| **employee** | Сотрудник | - Создаёт заявки<br>- Видит свои заявки<br>- Редактирует свои заявки (только статус "Создана") |
| **Исполнитель категории** | Пользователь в `category_executors` | - Видит все заявки категории<br>- Может взять в работу<br>- Управляет назначенными заявками |

### Проверки Policy:

```php
// Редактирование
if ($ticket->status_id === TicketStatus::COMPLETED->value) {
    return false; // Завершенные нельзя редактировать
}

// Удаление
if ($ticket->status_id === TicketStatus::COMPLETED->value) {
    return false; // Завершенные нельзя удалять
}
```

---

## 📱 Интеграция с Telegram

### Автоматические уведомления:

1. **При взятии в работу:**
   - Автор получает уведомление
   - Показывается кнопка "Подтвердить выполнение"

2. **При отправке на подтверждение:**
   - Автор получает уведомление с кнопками:
     - ✅ Подтвердить выполнение
     - ◀️ Вернуть в работу

3. **При назначении исполнителя (админом):**
   - Исполнитель получает уведомление
   - Статус автоматически меняется на "В работе" (если был "Создана")

---

## 💻 Frontend Implementation

### Vue.js - computed свойства:

```javascript
// Может взять в работу
const canTakeToWork = computed(() => {
    const statusCreated = props.ticket.status?.name === 'Создана'
        && !props.ticket.executor
        && props.isExecutorOfCategory;

    const statusPostponed = props.ticket.status?.name === 'Отложена'
        && (isExecutor.value || (!props.ticket.executor && props.isExecutorOfCategory));

    return statusCreated || statusPostponed;
});

// Может отложить
const canPostpone = computed(() => {
    const statusCreated = props.ticket.status?.name === 'Создана' && props.isExecutorOfCategory;
    const statusInProgress = isExecutor.value && props.ticket.status?.name === 'В работе';

    return statusCreated || statusInProgress;
});

// Завершена
const isCompleted = computed(() => {
    return props.ticket.status?.name === 'Завершена';
});

// Можно редактировать
const canEdit = computed(() => {
    return !isCompleted.value;
});
```

---

## 🎯 Примеры использования

### Сценарий 1: Стандартный workflow

```
1. Сотрудник создаёт заявку → Статус: "Создана"
2. Исполнитель видит заявку, нажимает "Взять в работу" → Статус: "В работе"
3. Исполнитель выполняет работу, нажимает "Отправить на подтверждение" → Статус: "Подтверждена"
4. Автор получает Telegram уведомление, проверяет работу, нажимает "Подтвердить выполнение" → Статус: "Завершена"
```

### Сценарий 2: С отложением

```
1. Заявка создана → Статус: "Создана"
2. Исполнитель видит, что нужны запчасти, нажимает "Отложить" → Статус: "Отложена"
3. Через 2 дня запчасти пришли, исполнитель нажимает "Взять в работу" → Статус: "В работе"
4. Исполнитель выполняет, нажимает "Отправить на подтверждение" → Статус: "Подтверждена"
5. Автор подтверждает → Статус: "Завершена"
```

### Сценарий 3: Админ назначает исполнителя

```
1. Заявка создана → Статус: "Создана"
2. Админ открывает заявку, видит панель управления (фиолетовую)
3. Админ нажимает "Назначить исполнителя", выбирает Иванова → Статус: "В работе"
4. Иванов получает уведомление в Telegram
5. Иванов выполняет и отправляет на подтверждение → Статус: "Подтверждена"
6. Автор подтверждает → Статус: "Завершена"
```

---

## 📝 Итоговая матрица кнопок

| Статус | Роль | Доступные кнопки |
|--------|------|------------------|
| **Создана** | Исполнитель категории | ✅ Взять в работу<br>⏸️ Отложить |
| **Создана** | Админ категории | 👤 Назначить исполнителя<br>🔄 Изменить статус |
| **В работе** | Исполнитель | ⏸️ Отложить<br>✉️ Отправить на подтверждение |
| **В работе** | Админ категории | ❌ Снять исполнителя<br>🔄 Изменить статус |
| **Подтверждена** | Автор | ✅ Подтвердить выполнение<br>◀️ Вернуть в работу |
| **Отложена** | Исполнитель | ✅ Взять в работу |
| **Отложена** | Исполнитель категории (если нет исполнителя) | ✅ Взять в работу |
| **Завершена** | Все | ❌ Нет кнопок<br>❌ Нельзя редактировать |

---

## 🚀 Следующие шаги

- [ ] Добавить историю изменений статусов
- [ ] Логировать все действия пользователей
- [ ] Добавить комментарии к заявкам
- [ ] Email уведомления (дополнительно к Telegram)
- [ ] Экспорт заявок в Excel/PDF
