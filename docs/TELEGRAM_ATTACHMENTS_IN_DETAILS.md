# 📎 Отправка вложений при просмотре деталей заявки

## Обзор

При нажатии на кнопку "👁 Подробнее" бот теперь отправляет не только текстовое описание заявки, но и все прикрепленные файлы (фото, видео, документы).

## Реализация

### Изменения в TelegramBotService

Добавлены три новых метода для отправки медиафайлов:

- `sendPhoto($chatId, $photo, $caption, $options)` - отправка фото
- `sendVideo($chatId, $video, $caption, $options)` - отправка видео
- `sendDocument($chatId, $document, $caption, $options)` - отправка документов

Все методы поддерживают:
- Отправку по `file_id` (быстро, без повторной загрузки)
- HTML форматирование в подписях
- Дополнительные параметры через массив `$options`

### Изменения в CallbackQueryHandler

Метод `handleViewTicket()` теперь:

1. Загружает заявку с вложениями: `->with(['attachments'])`
2. Отправляет текстовое описание заявки
3. Проходит по всем вложениям и отправляет их по одному
4. Использует сохраненный `telegram_file_id` для быстрой отправки
5. Обрабатывает ошибки отправки и логирует их

## Пример использования

```php
// Пользователь нажимает кнопку "👁 Подробнее" на заявке с вложениями
// Бот отправит:

📋 Детали заявки #123

📁 Категория: IT поддержка
🏪 Магазин: Магазин 1
❗ Проблема: Не работает касса
📊 Статус: В работе
👤 Исполнитель: Иван Иванов

📄 Описание:
Касса 1 завис при выборе любимого товара

⏰ Создана: 18.11.2025 10:19

// Затем бот отправит все прикрепленные файлы:
[📎 Видео с описанием проблемы]
[📎 Фото кассы]
[📎 Документ с логами]
```

## Типы поддерживаемых вложений

- **photo** - фотографии (отправляются как фото с сжатием)
- **video** - видеофайлы
- **document** - любые другие файлы (PDF, Excel, Word и т.д.)

## Преимущества использования telegram_file_id

При создании заявки через Telegram бот сохраняет `telegram_file_id` в базу данных. Это позволяет:

- ⚡ Быстрая отправка без повторной загрузки файла
- 💾 Экономия трафика
- 🔄 Возможность пересылать файлы неограниченное количество раз

## Обработка ошибок

Если при отправке вложения произойдет ошибка:
- Ошибка будет залогирована в `storage/logs/laravel.log`
- Отправка других вложений продолжится
- Пользователь увидит хотя бы текстовое описание заявки

## Логирование

Все ошибки отправки вложений логируются:

```php
Log::error('Failed to send attachment', [
    'ticket_id' => $ticketId,
    'attachment_id' => $attachment->id,
    'error' => $e->getMessage(),
]);
```

## Дальнейшие улучшения

- [ ] Группировка медиа в альбомы (sendMediaGroup) для более компактного отображения
- [ ] Предпросмотр документов с иконками типов файлов
- [ ] Кнопки для скачивания файлов напрямую из веб-интерфейса
- [ ] Сжатие больших видео перед отправкой
