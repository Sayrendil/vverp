# 🌐 Управление заявками через веб-интерфейс

## Обзор

Добавлен полный workflow управления заявками через веб-интерфейс, аналогичный функционалу Telegram бота.

## 🎯 Возможности

### Для исполнителей

#### 1. Взять заявку в работу
- **Когда доступно**: Заявка в статусе "Создана" и еще не взята
- **Условие**: Пользователь является исполнителем категории заявки
- **Результат**: Статус меняется на "В работе", пользователь назначается исполнителем

#### 2. Отложить заявку
- **Когда доступно**: Заявка в работе у текущего пользователя
- **Результат**: Статус меняется на "Отложена"

#### 3. Отправить на подтверждение
- **Когда доступно**: Заявка в работе у текущего пользователя
- **Результат**: Статус меняется на "Подтверждена"

### Для авторов заявок

#### 4. Подтвердить выполнение
- **Когда доступно**: Заявка в статусе "Подтверждена"
- **Условие**: Пользователь является автором заявки
- **Результат**: Статус меняется на "Завершена"

#### 5. Вернуть в работу
- **Когда доступно**: Заявка в статусе "Подтверждена"
- **Условие**: Пользователь является автором заявки
- **Результат**: Статус меняется обратно на "В работе"

## 📋 Workflow диаграмма

```
🆕 СОЗДАНА
    ↓
    [✅ Взять в работу] ← Исполнитель категории
    ↓
⚙️ В РАБОТЕ
    ↓
    [⏸️ Отложить] или [✅ На подтверждение] ← Исполнитель
    ↓
✅ НА ПОДТВЕРЖДЕНИИ
    ↓
    [✅ Подтвердить] или [❌ Вернуть в работу] ← Автор
    ↓
🎉 ЗАВЕРШЕНА
```

## 🎨 UI/UX

### Кнопки действий

Кнопки отображаются в отдельном блоке "Действия с заявкой" между основной информацией и вложениями.

- **Зеленые кнопки** (Primary): Положительные действия (Взять в работу, Подтвердить)
- **Серые кнопки** (Secondary): Нейтральные действия (Отложить)
- **Красные кнопки** (Danger): Возврат/отмена (Вернуть в работу)

### Подсказки

Под кнопками отображается информационное сообщение с объяснением доступных действий:
- Для исполнителей: что они могут сделать с заявкой
- Для авторов: как подтвердить выполнение или вернуть заявку

## 💻 Технические детали

### Backend (Laravel)

#### API Endpoints

```php
POST /tickets/{id}/take-to-work           // Взять в работу
POST /tickets/{id}/postpone               // Отложить
POST /tickets/{id}/send-for-confirmation  // Отправить на подтверждение
POST /tickets/{id}/confirm-completion     // Подтвердить выполнение
POST /tickets/{id}/return-to-work         // Вернуть в работу
```

#### TicketController методы

```php
public function takeToWork(string $id)
public function postpone(string $id)
public function sendForConfirmation(string $id)
public function confirmCompletion(string $id)
public function returnToWork(string $id)
```

### Frontend (Vue.js)

#### Props

```javascript
{
    ticket: Object,                  // Данные заявки
    isExecutorOfCategory: Boolean    // Является ли пользователь исполнителем категории
}
```

#### Computed Properties

```javascript
canTakeToWork         // Может ли взять в работу
canPostpone           // Может ли отложить
canSendForConfirmation // Может ли отправить на подтверждение
canConfirmCompletion  // Может ли подтвердить выполнение
canReturnToWork       // Может ли вернуть в работу
```

## 🔐 Права доступа

### Проверки на Backend

1. **Взять в работу**:
   - Заявка не должна иметь исполнителя
   - Пользователь должен быть исполнителем категории

2. **Отложить / На подтверждение**:
   - Пользователь должен быть исполнителем заявки

3. **Подтвердить / Вернуть в работу**:
   - Пользователь должен быть автором заявки

### Проверки на Frontend

Кнопки отображаются только если:
- Пользователь имеет соответствующие права
- Заявка находится в подходящем статусе
- `isExecutorOfCategory` = true (для взятия в работу)

## 📱 Пример использования

### Сценарий 1: Исполнитель берет заявку в работу

1. Исполнитель открывает заявку в статусе "Создана"
2. Видит блок "Действия с заявкой" с кнопкой "✅ Взять в работу"
3. Нажимает кнопку
4. Получает уведомление "Заявка взята в работу"
5. Статус меняется на "В работе", исполнитель назначен

### Сценарий 2: Исполнитель завершает работу

1. Исполнитель видит свою заявку в работе
2. Видит кнопки "⏸️ Отложить" и "✅ Отправить на подтверждение"
3. Нажимает "Отправить на подтверждение"
4. Статус меняется на "Подтверждена"
5. Автор получает уведомление (через Telegram)

### Сценарий 3: Автор подтверждает выполнение

1. Автор открывает свою заявку в статусе "Подтверждена"
2. Видит кнопки "✅ Подтвердить выполнение" и "❌ Вернуть в работу"
3. Нажимает "Подтвердить выполнение"
4. Статус меняется на "Завершена"
5. Исполнитель получает уведомление (через Telegram)

## 🎨 Скриншоты компонентов

### Блок действий для исполнителя (заявка создана)

```
┌─────────────────────────────────────────┐
│ ⚡ Действия с заявкой                   │
│                                         │
│ [✅ Взять в работу]                     │
│                                         │
│ ℹ️ Доступные действия: Вы можете       │
│    взять эту заявку в работу.          │
└─────────────────────────────────────────┘
```

### Блок действий для исполнителя (заявка в работе)

```
┌─────────────────────────────────────────┐
│ ⚡ Действия с заявкой                   │
│                                         │
│ [⏸️ Отложить] [✅ На подтверждение]    │
│                                         │
│ ℹ️ Вы исполнитель: Можете отложить     │
│    заявку или отправить на             │
│    подтверждение автору.               │
└─────────────────────────────────────────┘
```

### Блок действий для автора (заявка на подтверждении)

```
┌─────────────────────────────────────────┐
│ ⚡ Действия с заявкой                   │
│                                         │
│ [✅ Подтвердить] [❌ Вернуть в работу]  │
│                                         │
│ ℹ️ Вы автор: Можете подтвердить        │
│    выполнение или вернуть заявку       │
│    в работу.                           │
└─────────────────────────────────────────┘
```

## 🔄 Синхронизация с Telegram

Workflow в веб-интерфейсе **полностью синхронизирован** с Telegram:
- Изменения статуса через веб вызывают события (TicketStatusChanged)
- Events триггерят отправку уведомлений в Telegram
- Пользователи получают уведомления о всех изменениях
- Действия можно выполнять как через веб, так и через Telegram

## 🚀 Будущие улучшения

- [ ] Модальные окна подтверждения для критических действий
- [ ] История изменений статуса в timeline
- [ ] Комментарии при изменении статуса
- [ ] Автоматическое обновление страницы при изменениях (WebSockets)
- [ ] Уведомления в браузере через Push API
