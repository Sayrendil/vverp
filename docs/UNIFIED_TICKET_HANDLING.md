# 🔄 Унифицированная обработка заявок из Telegram и Web

## Обзор изменений

Система теперь обрабатывает заявки **одинаково**, независимо от источника создания (Telegram или Web).

## Что было изменено

### 1. ✅ Удалено разделение по источнику создания

**Ранее:**
- Уведомления автору отправлялись только для заявок из Web
- Проверка `if ($ticket->created_via === 'telegram') return;` блокировала уведомления

**Теперь:**
- Уведомления отправляются **всегда**, независимо от источника
- Поле `created_via` сохраняется для статистики, но не влияет на логику

### 2. 📎 Унифицирована отправка вложений

**Для заявок из Telegram:**
- Используется `telegram_file_id` для быстрой отправки через Telegram API

**Для заявок из Web:**
- Генерируется публичный URL: `url('storage/' . $attachment->file_path)`
- Файл отправляется через Telegram Bot API по URL

**Поддерживаемые типы:**
- `photo` → `sendPhoto()`
- `video` → `sendVideo()`
- `document` → `sendDocument()`

### 3. 🔔 Исполнители получают вложения при уведомлении

**Было:**
```php
// Только текстовое сообщение
$this->bot->sendMessage($executor->telegram_id, $message, $buttons);
```

**Стало:**
```php
// Сообщение + вложения
$this->bot->sendMessage($executor->telegram_id, $message, $buttons);
$this->sendAttachments($ticket, $executor->telegram_id);
```

## Измененные файлы

### 1. `app/Listeners/SendTicketCreatedTelegramNotification.php`

```php
// ❌ УДАЛЕНО
if ($ticket->created_via === 'telegram') {
    return;
}

// ✅ Теперь уведомления отправляются всегда
$user = $ticket->author;
if (!$user->telegram_id) {
    return;
}
```

**Добавлен импорт:**
```php
use App\Models\Ticket;
```

### 2. `app/Listeners/SendTicketCreatedNotificationToExecutors.php`

**Добавлен метод `sendAttachments()`:**

```php
private function sendAttachments($ticket, string $telegramId): void
{
    foreach ($attachments as $attachment) {
        // Telegram файлы
        if ($attachment->telegram_file_id) {
            match($attachment->file_type) {
                'photo' => $this->bot->sendPhoto(...),
                'video' => $this->bot->sendVideo(...),
                'document' => $this->bot->sendDocument(...),
            };
        }
        // Web файлы
        else if ($attachment->file_path) {
            $publicUrl = url('storage/' . $attachment->file_path);
            match($attachment->file_type) {
                'photo' => $this->bot->sendPhoto($telegramId, $publicUrl),
                // ...
            };
        }
    }
}
```

### 3. `docker-compose.prod.yml`

```yaml
# ❌ Старый порт
ports:
  - "8041:80"

# ✅ Новый порт
ports:
  - "80:80"
```

## Требования к серверу

### 🔗 Символическая ссылка storage

Для работы публичных URL файлов **обязательно** должна быть настроена символическая ссылка:

```bash
# Внутри контейнера
sudo docker exec vverp_app php artisan storage:link
```

Проверка:
```bash
sudo docker exec vverp_app ls -la public/ | grep storage
# Должно быть: storage -> /var/www/storage/app/public
```

### 🌐 Доступность файлов

Telegram Bot API должен иметь возможность скачать файлы по URL:
- ✅ `http://10.193.0.55/storage/uploads/file.jpg`
- ✅ `http://yourdomain.com/storage/uploads/file.jpg`

**Важно:** Сервер должен быть доступен из интернета или локальной сети Telegram серверов.

## Порядок развертывания

### 1. Обновление на сервере

```bash
cd /home/erp/vverp
git pull

# Перезапуск контейнеров с новым портом
sudo docker compose -f docker-compose.prod.yml down
sudo docker compose -f docker-compose.prod.yml up -d

# Очистка кэша
sudo docker exec vverp_app php artisan config:clear
sudo docker exec vverp_app php artisan cache:clear
sudo docker exec vverp_app php artisan route:clear
sudo docker exec vverp_app php artisan view:clear

# Проверка storage link
sudo docker exec vverp_app ls -la public/ | grep storage
```

### 2. Проверка порта

```bash
# Сервер теперь доступен на порту 80
curl http://10.193.0.55/
# Вместо http://10.193.0.55:8041/
```

### 3. Тестирование

**Тест 1: Создание заявки через Web**
1. Создайте заявку через веб с вложением
2. Проверьте, что автор получил уведомление в Telegram
3. Проверьте, что исполнители получили уведомление + вложение

**Тест 2: Создание заявки через Telegram**
1. Создайте заявку через бота с вложением
2. Проверьте, что автор получил уведомление
3. Проверьте, что исполнители получили уведомление + вложение

**Тест 3: Просмотр деталей**
1. Нажмите "👁 Подробнее" на любой заявке
2. Должны отобразиться все вложения (и Telegram, и Web)

## Логирование

Все операции логируются:

```php
// Успешная отправка
Log::info('Notification sent to executor', [
    'ticket_id' => $ticket->id,
    'executor_id' => $executor->id,
]);

// Файл не найден
Log::warning('Attachment file not found', [
    'ticket_id' => $ticket->id,
    'file_path' => $filePath,
]);

// Ошибка отправки
Log::error('Failed to send attachment', [
    'ticket_id' => $ticket->id,
    'error' => $e->getMessage(),
]);
```

Просмотр логов:
```bash
sudo docker exec vverp_app tail -f storage/logs/laravel.log
```

## Преимущества новой системы

✅ **Единая логика** - не важно откуда пришла заявка
✅ **Автоматические уведомления** - всегда и везде
✅ **Вложения работают** - из любого источника
✅ **Прозрачность** - все действия логируются
✅ **Стандартный порт** - 80 вместо 8041

## Возможные проблемы

### ❌ Вложения не отправляются

**Причина:** Нет storage link
**Решение:**
```bash
sudo docker exec vverp_app php artisan storage:link
```

### ❌ 404 на файлы

**Причина:** Неверные права доступа
**Решение:**
```bash
sudo docker exec -u root vverp_app chown -R appuser:appgroup /var/www/storage
sudo docker exec -u root vverp_app chmod -R 775 /var/www/storage
```

### ❌ Telegram не может скачать файл

**Причина:** Сервер недоступен из интернета
**Решение:** Убедитесь что порт 80 открыт и сервер доступен

### ❌ Порт 80 занят

**Причина:** Другой сервис использует порт 80
**Решение:**
```bash
# Найти процесс
sudo lsof -i :80

# Остановить конфликтующий сервис
sudo systemctl stop nginx  # или apache2
```

## Мониторинг

```bash
# Проверка работы контейнеров
sudo docker ps

# Логи Telegram бота
sudo docker logs -f vverp_queue

# Логи веб-сервера
sudo docker logs -f vverp_nginx

# Логи приложения
sudo docker exec vverp_app tail -f storage/logs/laravel.log
```

---

**Дата обновления:** 2025-11-18
**Версия:** 2.0
**Статус:** ✅ Готово к продакшену
