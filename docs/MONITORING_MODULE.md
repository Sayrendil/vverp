# 🖥️ Модуль мониторинга доступности хостов

## 📋 Описание

Полноценная альтернатива Zabbix для мониторинга доступности хостов (касс, серверов) на основе Laravel. Система выполняет периодическую проверку доступности через ping, собирает статистику и предоставляет удобный интерфейс управления.

## 🎯 Возможности

- ✅ Мониторинг неограниченного количества хостов
- ✅ Привязка хостов к магазинам
- ✅ Автоматические проверки по расписанию через Laravel Scheduler
- ✅ Асинхронная обработка через очереди (Queue)
- ✅ Сбор статистики: uptime, время отклика, потери пакетов
- ✅ Выявление проблемных хостов
- ✅ Гибкая настройка интервалов проверки для каждого хоста
- ✅ Веб-интерфейс для управления и просмотра статистики
- ✅ CLI команды для управления через терминал

## 📦 Компоненты системы

### База данных

#### Таблица `hosts`
- `id` - ID хоста
- `store_id` - ID магазина (внешний ключ)
- `name` - Название хоста (Касса 1, Касса 2, Сервер и т.д.)
- `ip_address` - IP адрес или hostname для проверки
- `description` - Описание хоста
- `is_active` - Активен ли мониторинг (boolean)
- `check_interval` - Интервал проверки в минутах (по умолчанию 5)
- `timeout` - Таймаут пинга в секундах (по умолчанию 3)

#### Таблица `host_availability_logs`
- `id` - ID лога
- `host_id` - ID хоста (внешний ключ)
- `is_available` - Доступен ли хост (boolean)
- `response_time` - Время отклика в миллисекундах
- `packet_loss` - Процент потери пакетов (0-100)
- `error_message` - Сообщение об ошибке
- `checked_at` - Время проверки

### Модели

- **Host** (`app/Models/Host.php`) - Модель хоста
- **HostAvailabilityLog** (`app/Models/HostAvailabilityLog.php`) - Модель лога проверки

### Jobs

- **CheckHostAvailability** (`app/Jobs/CheckHostAvailability.php`)
  - Выполняет ping хоста
  - Поддерживает Windows и Linux/Unix
  - Записывает результаты в базу данных
  - Очередь: `monitoring`
  - Попытки: 2
  - Таймаут: 30 секунд

### Сервисы

- **MonitoringService** (`app/Services/MonitoringService.php`)
  - Централизованная бизнес-логика мониторинга
  - Запуск проверок
  - Получение статистики
  - Поиск проблемных хостов
  - Очистка старых логов

### Команды

#### `php artisan monitoring:check-hosts`
Проверить доступность хостов.

**Опции:**
- `--all` - Проверить все хосты, игнорируя расписание
- `--store=ID` - Проверить хосты конкретного магазина
- `--sync` - Выполнить синхронно, без очереди

**Примеры:**
```bash
# Проверка по расписанию (только те хосты, которым пора)
php artisan monitoring:check-hosts

# Проверка всех активных хостов
php artisan monitoring:check-hosts --all

# Проверка хостов магазина ID=5
php artisan monitoring:check-hosts --store=5

# Синхронная проверка (для тестирования)
php artisan monitoring:check-hosts --all --sync
```

#### `php artisan monitoring:stats`
Просмотр статистики мониторинга.

**Опции:**
- `--days=N` - Количество дней для анализа (по умолчанию 7)
- `--host=ID` - Статистика по конкретному хосту

**Примеры:**
```bash
# Общая статистика за 7 дней
php artisan monitoring:stats

# Общая статистика за 30 дней
php artisan monitoring:stats --days=30

# Статистика по хосту ID=10
php artisan monitoring:stats --host=10 --days=7
```

#### `php artisan monitoring:problematic`
Показать список проблемных хостов.

**Опции:**
- `--checks=N` - Количество последних проверок для анализа (по умолчанию 10)

**Пример:**
```bash
# Найти проблемные хосты (анализ последних 10 проверок)
php artisan monitoring:problematic

# Анализ последних 20 проверок
php artisan monitoring:problematic --checks=20
```

#### `php artisan monitoring:clean-logs`
Очистить старые логи мониторинга.

**Опции:**
- `--days=N` - Удалить логи старше N дней (по умолчанию 30)

**Пример:**
```bash
# Удалить логи старше 30 дней
php artisan monitoring:clean-logs

# Удалить логи старше 90 дней
php artisan monitoring:clean-logs --days=90
```

## 🚀 Развертывание

### 1. Запустить миграции

```bash
php artisan migrate
```

### 2. Добавить хосты через интерфейс

Перейдите в **Справочники → Хосты для мониторинга** и добавьте хосты для мониторинга.

### 3. Настроить Queue Worker

Система использует очередь `monitoring` для асинхронной обработки проверок.

**Development:**
```bash
php artisan queue:work --queue=monitoring,default
```

**Production (Supervisor):**

Создайте файл `/etc/supervisor/conf.d/vverp-monitoring-queue.conf`:

```ini
[program:vverp-monitoring-queue]
process_name=%(program_name)s_%(process_num)02d
command=php /home/erp/vverp/artisan queue:work --queue=monitoring --sleep=3 --tries=2 --max-time=3600
autostart=true
autorestart=true
stopasgroup=true
killasgroup=true
user=erp
numprocs=2
redirect_stderr=true
stdout_logfile=/home/erp/vverp/storage/logs/monitoring-queue.log
stopwaitsecs=3600
```

Перезагрузите Supervisor:
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start vverp-monitoring-queue:*
```

### 4. Настроить Scheduler

Scheduler уже настроен в `routes/console.php`:

- **Каждые 5 минут** - проверка хостов по расписанию
- **Каждую ночь в 03:00** - очистка старых логов (старше 30 дней)

Убедитесь, что cron настроен для запуска Scheduler:

```bash
* * * * * cd /home/erp/vverp && php artisan schedule:run >> /dev/null 2>&1
```

## 📊 Использование

### Веб-интерфейс

#### Управление хостами
**Справочники → Хосты для мониторинга**

- Просмотр всех хостов
- Добавление новых хостов
- Редактирование параметров мониторинга
- Удаление хостов

#### Дашборд мониторинга
**Мониторинг → Дашборд** (`/monitoring`)

- Общая статистика: uptime, количество проверок, проблемные хосты
- Список всех хостов с текущим статусом
- Кнопки для ручной проверки

#### Детальная статистика по хосту
**Мониторинг → Детали хоста** (`/monitoring/hosts/{id}`)

- Детальная статистика за период
- График доступности
- История проверок
- Время отклика

### Программный доступ

```php
use App\Services\MonitoringService;

$monitoring = app(MonitoringService::class);

// Проверить все активные хосты
$monitoring->checkAllActiveHosts();

// Проверить хосты магазина
$monitoring->checkStoreHosts($storeId);

// Проверить конкретный хост
$monitoring->checkHost($hostId);

// Получить статистику
$stats = $monitoring->getOverallStatistics(days: 7);

// Получить проблемные хосты
$problematic = $monitoring->getProblematicHosts(checksCount: 10);

// Очистить старые логи
$monitoring->cleanOldLogs(days: 30);
```

## ⚙️ Конфигурация

### Интервалы проверки

Для каждого хоста можно настроить индивидуальный интервал проверки (поле `check_interval` в минутах).

**Рекомендуемые значения:**
- Критичные системы: **1-5 минут**
- Обычные системы: **5-15 минут**
- Некритичные системы: **30-60 минут**

### Таймаут ping

Для каждого хоста можно настроить таймаут ожидания ответа (поле `timeout` в секундах).

**Рекомендуемые значения:**
- Локальная сеть: **1-3 секунды**
- Интернет/VPN: **3-10 секунд**

### Очередь мониторинга

Очередь `monitoring` обрабатывается отдельными worker'ами для изоляции от основных задач.

**Настройки в Job:**
- Попытки: 2
- Таймаут: 30 секунд
- Backoff: 5, 15 секунд

## 📈 Метрики и статистика

### Общая статистика
- Всего хостов / Активных / Неактивных
- Всего проверок за период
- Успешных / Неудачных проверок
- Uptime % (процент доступности)
- Среднее время отклика
- Количество проблемных хостов

### Статистика по хосту
- Uptime % за период
- Среднее / Мин / Макс время отклика
- Средняя потеря пакетов
- История проверок
- Почасовая статистика

### Проблемные хосты

Хост считается проблемным, если более 50% последних проверок показали недоступность.

## 🔧 Обслуживание

### Автоматическая очистка логов

Scheduler автоматически очищает логи старше 30 дней каждую ночь в 03:00.

### Ручная очистка логов

```bash
# Удалить логи старше 30 дней
php artisan monitoring:clean-logs --days=30

# Удалить логи старше 90 дней
php artisan monitoring:clean-logs --days=90
```

### Мониторинг очереди

```bash
# Проверить статус очереди
php artisan queue:monitor monitoring

# Просмотреть failed jobs
php artisan queue:failed

# Повторить failed job
php artisan queue:retry <job-id>

# Повторить все failed jobs
php artisan queue:retry all
```

## 🐛 Решение проблем

### Проверки не выполняются

1. **Проверьте Scheduler:**
```bash
php artisan schedule:list
```

2. **Проверьте cron:**
```bash
crontab -l
```

3. **Проверьте Queue Worker:**
```bash
sudo supervisorctl status vverp-monitoring-queue:*
```

### Неверные результаты ping

1. **Проверьте права доступа** - команда ping может требовать особых прав
2. **Проверьте сетевую доступность** - попробуйте выполнить ping вручную
3. **Увеличьте таймаут** - для медленных соединений

### Большое количество failed jobs

1. **Проверьте логи:**
```bash
tail -f storage/logs/laravel.log
```

2. **Проверьте сетевую доступность** хостов
3. **Увеличьте таймаут** в настройках хоста

## 📝 Примеры использования

### Добавление хостов через Seeder

```php
use App\Models\Host;
use App\Models\Store;

// Создаем хосты для магазина
$store = Store::where('name', 'Магазин №1')->first();

Host::create([
    'store_id' => $store->id,
    'name' => 'Касса 1',
    'ip_address' => '192.168.1.100',
    'description' => 'Основная касса',
    'is_active' => true,
    'check_interval' => 5,
    'timeout' => 3,
]);

Host::create([
    'store_id' => $store->id,
    'name' => 'Касса 2',
    'ip_address' => '192.168.1.101',
    'description' => 'Резервная касса',
    'is_active' => true,
    'check_interval' => 5,
    'timeout' => 3,
]);
```

### Ручная проверка через API

```javascript
// Vue/JavaScript пример
async function checkHost(hostId) {
    try {
        const response = await axios.post(`/monitoring/check-host/${hostId}`);
        alert(response.data.message);
    } catch (error) {
        alert('Ошибка при проверке хоста');
    }
}
```

## 🎨 Интеграция с интерфейсом

Система полностью интегрирована с существующей системой справочников:

- **Навигация:** Справочники → Хосты для мониторинга
- **CRUD операции:** Через универсальный DictionaryController
- **Дополнительно:** Отдельный раздел "Мониторинг" для статистики и дашборда

## 🔐 Права доступа

Все функции мониторинга доступны только **администраторам** (middleware `EnsureUserIsAdmin`).

## 📊 Производительность

- **Одна проверка:** ~1-5 секунд (в зависимости от таймаута)
- **100 хостов:** ~5-10 минут через очередь (при 2 worker'ах)
- **Хранение логов:** Рекомендуется хранить не более 30 дней для оптимизации

## 🔮 Будущие улучшения

- [ ] Уведомления в Telegram при недоступности
- [ ] Email уведомления
- [ ] Графики доступности в веб-интерфейсе
- [ ] Проверка через HTTP/HTTPS (не только ping)
- [ ] Проверка портов (TCP/UDP)
- [ ] Экспорт отчетов в PDF/Excel
- [ ] Dashboard виджеты для главной страницы
- [ ] Интеграция с Prometheus/Grafana

## 📚 Связанные документы

- [QUEUE_SETUP.md](QUEUE_SETUP.md) - Настройка очередей
- [SUPERVISOR.md](SUPERVISOR.md) - Настройка Supervisor
- [DEPLOYMENT-TESTS.md](DEPLOYMENT-TESTS.md) - Тесты развертывания

---

**Автор:** AI Assistant
**Дата создания:** 2025-11-19
**Версия:** 1.0.0
